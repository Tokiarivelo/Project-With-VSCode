@model IEnumerable<VenteProduit.Models.Facturation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PrixTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Achat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PrixTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Achat.AchatId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FacturationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FacturationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FacturationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
